services:
  caddy:
    build: .
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./etc_caddy:/etc/caddy
      - /var/run/tailscale/tailscaled.sock:/var/run/tailscale/tailscaled.sock
        
